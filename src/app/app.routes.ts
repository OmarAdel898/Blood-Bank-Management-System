import { Routes } from '@angular/router';
import { DonorsDashboardComponent } from './donors-dashboard/donors-dashboard.component';

export const routes: Routes = [
  { path: '', component: DonorsDashboardComponent },
];
